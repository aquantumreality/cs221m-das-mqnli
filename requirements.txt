# CS221M Mechanistic Interpretability project dependencies.
#
# Core deep-learning + HuggingFace stack.
torch>=2.1
transformers>=4.40
datasets>=2.14
tokenizers>=0.15

# Intervention / causal-abstraction toolkit.
# pyvene is the library used in the Stanford NLP DAS tutorials.
pyvene>=0.1.4

# Numerics, dataframes, basic ML utilities.
numpy>=1.24
pandas>=2.0
scikit-learn>=1.3

# Plotting (we only really need matplotlib; seaborn is optional but nicer).
matplotlib>=3.7
seaborn>=0.13

# Convenience.
tqdm>=4.66
pyyaml>=6.0
